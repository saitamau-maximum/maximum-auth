import { RegisterForm } from "..";

import { Meta } from "@storybook/react";

const meta = {
	title: "Feature/Auth/Components/RegisterForm",
} satisfies Meta;

export default meta;

export const Overview = () => <RegisterForm />;
