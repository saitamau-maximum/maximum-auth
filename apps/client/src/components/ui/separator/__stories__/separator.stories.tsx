import { Meta } from "@storybook/react";
import { Separator } from "..";

const meta = {
	title: "Component/UI/Separator",
} satisfies Meta;

export default meta;

export const Overview = () => <Separator />;
