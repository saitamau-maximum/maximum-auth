import { RegisterPage } from "../page";

import { Meta } from "@storybook/react";

const meta = {
	title: "Feature/Auth/RegisterPagePage",
} satisfies Meta;

export default meta;

export const Overview = () => <RegisterPage />;
